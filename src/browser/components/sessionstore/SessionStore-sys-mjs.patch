diff --git a/browser/components/sessionstore/SessionStore.sys.mjs b/browser/components/sessionstore/SessionStore.sys.mjs
index 5414adeca242f00bff98c5e41de74f33dd2846bb..a6324da568873029660c33d8b0a933c34bd599e9 100644
--- a/browser/components/sessionstore/SessionStore.sys.mjs
+++ b/browser/components/sessionstore/SessionStore.sys.mjs
@@ -6699,6 +6699,17 @@ var SessionStoreInternal = {
       tab.setAttribute("zen-workspace-id", tabData.zenWorkspace);
     }
 
+    // Restore Limb tree metadata (persistence.md S1.3)
+    if (tabData.limbNodeId) {
+      tab.setAttribute("limb-node-id", tabData.limbNodeId);
+    }
+    if (tabData.limbTreeParentId) {
+      tab.setAttribute("limb-tree-parent-id", tabData.limbTreeParentId);
+    }
+    if (tabData.limbTreeCreatedAt) {
+      tab.setAttribute("limb-tree-created-at", tabData.limbTreeCreatedAt);
+    }
+
     if (tabData.pinned) {
       tabbrowser.pinTab(tab);
     } else {
